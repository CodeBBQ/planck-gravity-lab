# Worker Records

Store one durable file per allocated worker ID, for example `WRK-0001.md`. Never delete completed, failed, cancelled, blocked, or superseded worker history merely because the chat ended.
