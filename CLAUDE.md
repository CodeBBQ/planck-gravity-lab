# Claude Repository Instructions

Read `AGENTS.md` and the top-level `README.md` first. Repository research, provenance, orchestration, independence, and issue-specific protocols remain authoritative and must never be weakened by Project Cockpit integration.

This repository is tracked in `CodeBBQ/ProjectCockpit` as **Planck Gravity Lab** (`projects/planck-gravity-lab.md`). On the first meaningful interaction in a fresh context, perform repository-first startup and inspect durable audit/orchestration state, issues, recent work, and validation evidence before forming a management-level Project State Assessment.

Use project-management language and exactly one primary `next_action`. Never invent progress, validation, blockers, GO/NO-GO state, or milestone completion. Do not infer research readiness from cockpit metadata.

When asked `Update Project Cockpit for the current project`, refresh the assessment and, if `CodeBBQ/ProjectCockpit` is writable, update its project snapshot, `PROJECTS.md`, and `CHANGELOG.md`; otherwise return the full assessment as a handoff and state that the cockpit was not modified.